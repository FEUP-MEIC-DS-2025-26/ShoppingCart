// webhook.hmac.test.js neutralized: webhook ingestion (HMAC-validated endpoint)
// has been removed from this service. Placeholder kept for test runner compatibility.
describe('HMAC placeholder', () => {
  it('placeholder', () => {});
});
