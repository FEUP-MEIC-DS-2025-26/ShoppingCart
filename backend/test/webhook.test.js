describe('Jumpseller webhook integration', function () {
// webhook.test.js removed — webhook ingestion replaced/disabled in this service.
// Placeholder kept for test runner compatibility.
describe('Webhook placeholder', () => {
  it('placeholder', () => {});
});
// Ensure a clean DB for the test (only once across loaded test files)
